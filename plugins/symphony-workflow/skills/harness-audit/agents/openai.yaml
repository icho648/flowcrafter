interface:
  display_name: "Harness Audit"
  short_description: "Audit repo-local harness evidence, checks, and drift"
  default_prompt: "Use $harness-audit to audit this repository's Agent guidance, evidence, checks, and drift."
policy:
  allow_implicit_invocation: false
