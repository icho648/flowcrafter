interface:
  display_name: "PR 自动驾驶"
  short_description: "循环处理冲突、Review 评论与 CI，让 PR 保持可合并状态"
  default_prompt: "使用 $autopilot 持续检查当前 PR，按优先级处理冲突、Review 评论和 CI，直到达到可合并状态。"

policy:
  allow_implicit_invocation: false
